# Setting batch to true, triggers one build at a time.
# if there is a push while a build in progress, it will wait,
# until the running build finishes, and produce a build with all the changes
# that happened during the last build.
trigger:
  batch: true
  branches:
    include:
    - feature/*
    - standalone-template
  paths:
    include:
    - '*'
    exclude:
    - eng/Version.Details.xml
    - .github/*
    - docs/*
    - CODE-OF-CONDUCT.md
    - LICENSE.TXT
    - PATENTS.TXT
    - README.md
    - SECURITY.md
    - THIRD-PARTY-NOTICES.TXT

pr:
  branches:
    include:
    - feature/*
    - standalone-template
  paths:
    include:
    - '*'
    exclude:
    - eng/Version.Details.xml
    - .github/*
    - docs/*
    - CODE-OF-CONDUCT.md
    - LICENSE.TXT
    - PATENTS.TXT
    - README.md
    - SECURITY.md
    - THIRD-PARTY-NOTICES.TXT

tages:
- stage: build
  displayName: build
  jobs:
  - template: eng/templates/build-job.yml
    parameters:
      name: Windows_NT_x64
      displayName: Windows_NT x64
      osGroup: Windows_NT
      archType: x64
      pool:
        vmImage: 'windows-latest'

  - template: eng/templates/build-job.yml
    parameters:
      name: OSX_x64
      displayName: OSX x64
      osGroup: OSX
      archType: x64
      pool:
        vmImage: 'macOS-latest'

  - template: eng/templates/build-job.yml
    parameters:
      name: Linux_x64
      displayName: Linux x64
      osGroup: Linux
      archType: x64
      pool:
        vmImage: 'ubuntu-latest'
